# บันทึกการพัฒนา (NOTES)

บันทึกว่าทำอะไรไปบ้าง ออกแบบอย่างไร และทำไม — ใช้ประกอบการเขียนรายงานโปรเจกต์จบได้

วันที่เริ่ม: 23 ก.ย. 2569

---

## 1. สิ่งที่ได้รับเป็นโจทย์

- ผู้ใช้ 3 กลุ่ม: **นักศึกษา**, **อาจารย์**, **ฝ่ายทะเบียน**
- นักศึกษา 2 กลุ่ม: **4 ปี = 100 ชม.**, **เทียบโอน = 50 ชม.**
- Stack: PHP + Apache2 + MariaDB บน Docker Desktop
- เอกสารอ้างอิง (อยู่ในโฟลเดอร์ `docs/`)
  - `ทักษะวิชาชีพ.pdf` — รายชื่อทักษะ 5 ทักษะ ผู้ควบคุม ทักษะละ 25 ชม. และหมายเหตุ 7 ข้อ
  - `แบบบันทึกการฝึกทักษะวิชาชีพ.pdf` — แบบฟอร์มที่ต้องพิมพ์ (A4 แนวนอน)
- ต้องเผื่อรองรับ API ของ มทร.ศรีวิชัย ในอนาคต

## 2. สิ่งที่ทำไปแล้ว

### โครงสร้างพื้นฐาน
- [x] `docker-compose.yml` 3 services: `web` (php:8.3-apache), `db` (mariadb:11), `phpmyadmin`
- [x] `Dockerfile` ติดตั้ง pdo_mysql, gd, intl, mbstring, zip + เปิด mod_rewrite
- [x] Apache ชี้ DocumentRoot ไปที่ `public/` (โฟลเดอร์ `app/` เข้าจากเว็บไม่ได้)
- [x] ฐานข้อมูล utf8mb4 + timezone Asia/Bangkok (+07:00) ภาษาไทยไม่เพี้ยน
- [x] สร้างตาราง + ข้อมูลตัวอย่างอัตโนมัติครั้งแรกที่รัน (`database/init/`)
- [x] Front controller (`public/index.php`) + ตาราง route (`app/routes.php`) → URL สวย เช่น `/teacher/activities`

### ฐานข้อมูล (11 ตาราง)
| ตาราง | ใช้ทำอะไร |
|---|---|
| `users` | ผู้ใช้ทุกบทบาท, ลายเซ็นที่บันทึกไว้ของอาจารย์ |
| `students` | รหัสนักศึกษา, แผน 4 ปี/เทียบโอน, สาขา, ปีที่เข้า, อาจารย์ที่ปรึกษา |
| `semesters` | ภาคการศึกษา (ใช้กรองตอนพิมพ์ "ประจำภาคการศึกษาที่") |
| `skills` | รายชื่อทักษะ + ผู้รับผิดชอบ + ชม.สูงสุดต่อทักษะ (25) |
| `activities` | กิจกรรมที่อาจารย์สร้าง (`public` = เปิดสมัคร, `assigned` = มอบหมายเฉพาะคน) |
| `activity_sessions` | วัน/เวลาที่ต้องมาฝึก (หลายวันต่อกิจกรรม) |
| `participations` | การเข้าร่วม = **1 แถวในแบบบันทึก** (สถานะ, ชั่วโมง, ผ่าน/ไม่ผ่าน, ลายเซ็น) |
| `notifications` | การแจ้งเตือนในระบบ |
| `audit_logs` | บันทึกว่าใครทำอะไร |
| `settings` | ค่าที่ฝ่ายทะเบียนแก้ได้ (เกณฑ์ชั่วโมง, ชื่อหัวหน้าหลักสูตร ฯลฯ) |
| `api_keys` | key สำหรับระบบภายนอก (เก็บเป็น SHA-256 ไม่เก็บตัวจริง) |

### สถานะของการเข้าร่วม (participations.status)
```
นักศึกษาขอ ──► pending ──(อาจารย์อนุมัติ)──► approved ──(บันทึกผล+เซ็น)──► completed (ผ่าน/ไม่ผ่าน)
                  │                              ▲
                  └──(ปฏิเสธ)──► rejected         │
อาจารย์มอบหมาย ────────────────────────────────────┘ (เข้า approved ทันที)
นักศึกษายกเลิก / อาจารย์นำออก ──► cancelled
```

### ฝั่งนักศึกษา
- [x] ภาพรวมชั่วโมง: สะสม/เกณฑ์, แถบความคืบหน้า, ครบหรือยัง, ขาดอีกกี่ชม.
- [x] ชั่วโมงแยกตามทักษะ และแยกตามอาจารย์ ("มีของอาจารย์อะไรบ้าง")
- [x] หน้าขอเข้าร่วมกิจกรรม (ค้นหา/กรองตามทักษะ, เช็กที่นั่งเต็ม/ปิดรับ, ใส่ข้อความถึงอาจารย์)
- [x] ประวัติการเข้าร่วม (แยกแท็บตามสถานะ)
- [x] พิมพ์แบบบันทึกการฝึกทักษะวิชาชีพ

### ฝั่งอาจารย์
- [x] สร้าง/แก้ไข/ปิดรับ/ยกเลิก/ลบกิจกรรม พร้อมกำหนดหลายวัน-เวลา (ระบบรวมชั่วโมงจากตารางเวลาให้อัตโนมัติ)
- [x] อนุมัติ/ปฏิเสธคำขอ ทีละคนหรือเลือกหลายคน
- [x] มอบหมายกิจกรรมเฉพาะคน: ค้นหานักศึกษา (รหัส/ชื่อ) → กำหนดงาน ชั่วโมง วันเวลา
- [x] ค้นหานักศึกษา ดูรายละเอียดรายคน พิมพ์แบบฟอร์ม
- [x] บันทึกผล ผ่าน/ไม่ผ่าน + ชั่วโมงรายคน + หมายเหตุ
- [x] **ลายมือชื่ออาจารย์ผู้ควบคุม**: เลือก "ลายเซ็นออนไลน์" (วาดบน canvas / ใช้ลายเซ็นที่บันทึกไว้) หรือ "กรอกชื่อ"
- [x] หน้าบันทึกลายเซ็นประจำตัว

### ฝั่งฝ่ายทะเบียน
- [x] ภาพรวม: จำนวนนักศึกษา ครบ/ไม่ครบ แยกตามแผน ปีที่เข้า ชั่วโมงแยกตามทักษะ
- [x] ค้นหานักศึกษา (รหัส/ชื่อ, แผน, ปีที่เข้า, ครบ/ไม่ครบ) + พิมพ์แบบบันทึกเป็นหลักฐาน
- [x] ส่งออก CSV (มี BOM ให้ Excel อ่านภาษาไทยได้)
- [x] จัดการผู้ใช้ทุกบทบาท, ระงับ/เปิดบัญชี, รีเซ็ตรหัสผ่าน
- [x] นำเข้านักศึกษาจาก CSV (มีไฟล์ตัวอย่างให้ดาวน์โหลด)
- [x] จัดการรายชื่อทักษะ, ภาคการศึกษา, ตั้งค่าระบบ
- [x] ดูบันทึกการใช้งาน (audit log)

### แบบบันทึกการฝึกทักษะวิชาชีพ (หน้าพิมพ์)
- จัดหน้าให้เหมือน PDF ต้นฉบับ: โลโก้, ชื่อคณะ/มหาวิทยาลัย, "ประจำภาคการศึกษาที่", ☐ ทักษะวิชาชีพ 1/2,
  ชื่อ-สกุล-รหัส-สาขา, ตาราง 8 คอลัมน์ (ผลการฝึกทักษะ ผ่าน/ไม่ผ่าน), ช่องลงชื่อหัวหน้าหลักสูตร
- โลโก้ดึงออกมาจาก PDF ต้นฉบับ (`public/assets/img/logo.png`)
- เลือกพิมพ์เฉพาะภาค หรือทุกภาคได้, ติ๊กทักษะวิชาชีพ 1/2, ใส่ชื่อหัวหน้าหลักสูตร
- แบ่งหน้าอัตโนมัติ (ค่าเริ่มต้น 5 แถว/หน้า ปรับได้ในตั้งค่า), หน้าสุดท้ายมีสรุปชั่วโมง
- ใช้ `@page { size: A4 landscape }` → กด "พิมพ์" หรือเลือก "Save as PDF" ในเบราว์เซอร์

### API
- [x] Adapter เชื่อม API มหาวิทยาลัย (`none` / `mock` / `rmutsv`) — เปลี่ยนด้วย `.env` ไม่ต้องแก้โค้ด
- [x] เข้าสู่ระบบแบบ hybrid (ในระบบ → ถ้าไม่พบค่อยถาม API มหาวิทยาลัย แล้วสร้างบัญชีอัตโนมัติ)
- [x] หน้าทดสอบการเชื่อมต่อ / ดึง-นำเข้านักศึกษาจาก API (ฝ่ายทะเบียน)
- [x] ปุ่ม "ซิงก์จาก API มหาวิทยาลัย" ในหน้ารายละเอียดนักศึกษา
- [x] REST API v1 ของระบบเอง ให้ระบบมหาวิทยาลัยดึงชั่วโมงหรือส่งข้อมูลนักศึกษาเข้ามา

### สิ่งที่เพิ่มเอง (นอกเหนือจากโจทย์)
- ระบบแจ้งเตือนในเว็บ (กระดิ่งมุมขวาบน)
- บันทึกการใช้งาน (audit log) ทุกการกระทำสำคัญ
- นำเข้า CSV / ส่งออก CSV
- จำกัดชั่วโมงสูงสุดต่อทักษะ (ตามเอกสาร ทักษะละ 25 ชม.) — เปิด/ปิดได้ในตั้งค่า
- บังคับเปลี่ยนรหัสผ่านครั้งแรก (บัญชีที่นำเข้าจาก CSV ใช้รหัสนักศึกษาเป็นรหัสผ่านเริ่มต้น)
- จำกัดการเดารหัสผ่าน (ผิด 5 ครั้ง รอ 5 นาที)
- ภาคการศึกษา + ตั้งภาคปัจจุบัน
- phpMyAdmin สำหรับดูฐานข้อมูล
- รองรับมือถือ (เมนูพับได้)

## 3. ความปลอดภัยที่ทำไว้
- รหัสผ่านเก็บด้วย `password_hash` (bcrypt)
- ทุก query ใช้ prepared statement (กัน SQL injection)
- ทุกข้อความที่แสดงผ่าน `e()` = `htmlspecialchars` (กัน XSS)
- ทุกฟอร์ม POST มี CSRF token
- ตรวจสิทธิ์ตามบทบาททุกหน้า (`routes.php`) + ตรวจความเป็นเจ้าของกิจกรรม (อาจารย์แก้ได้เฉพาะของตัวเอง)
- นักศึกษาพิมพ์/ดูได้เฉพาะข้อมูลตัวเอง
- ลายเซ็นตรวจว่าเป็น PNG จริงก่อนบันทึก
- session cookie แบบ HttpOnly + SameSite=Lax, สร้าง session ใหม่หลังล็อกอิน

## 4. การตัดสินใจในการออกแบบ (และเหตุผล)
- **ไม่ใช้ framework** — ให้อ่าน/อธิบายโค้ดตอนสอบโปรเจกต์ได้ง่าย แต่ยังแยกชั้นชัดเจน (pages / services / integrations)
- **พิมพ์ด้วย HTML + CSS print** แทนการสร้าง PDF ฝั่งเซิร์ฟเวอร์ — ไม่ต้องติดตั้ง library/ฟอนต์ไทยเพิ่ม และผู้ใช้ "Save as PDF" ได้เอง
- **1 participation = 1 แถวในแบบฟอร์ม** — ตรงกับการที่อาจารย์ต้องเซ็นรับรองทีละรายการ
- **เก็บลายเซ็นเป็น data URL ในฐานข้อมูล** และ "คัดลอก" ลงแต่ละรายการตอนเซ็น — ถ้าอาจารย์เปลี่ยนลายเซ็นทีหลัง เอกสารเก่าไม่เปลี่ยนตาม
- **การนับชั่วโมง** อยู่ที่เดียว (`app/Services/HoursService.php`) — แก้กติกาที่เดียวมีผลทั้งระบบและ API

## 5. การทดสอบที่ทำแล้ว
- เปิดทุกหน้าของทั้ง 3 บทบาท (ตอบ 200 ไม่มี PHP error), ข้ามบทบาทได้ 403
- ขั้นตอนเต็ม: นักศึกษาขอเข้าร่วม → อาจารย์อนุมัติ → บันทึกผล + เซ็นออนไลน์ / กรอกชื่อ → แสดงในแบบบันทึกที่พิมพ์ → ชั่วโมงสะสมอัปเดต
- มอบหมายกิจกรรมเฉพาะคนหลายวัน, ค้นหานักศึกษาด้วยชื่อภาษาไทย
- POST ที่ไม่มี CSRF token ถูกปฏิเสธ (403)
- REST API: health, summary, records, students (X-API-Key และ Bearer), sync; key ผิดได้ 401
- นำเข้า CSV, ดึงนักศึกษาจาก mock API

## 6. สิ่งที่ควรทำต่อ (ข้อเสนอแนะ)
- ขอเอกสาร API จริงจากสำนักวิทยบริการฯ/ฝ่ายทะเบียน มทร.ศรีวิชัย แล้วแก้ `field_map` + endpoint ใน `.env`
- ถ้ามหาวิทยาลัยใช้ LDAP (Active Directory) ให้เพิ่ม driver ใหม่ที่ implement `UniversityApiInterface`
- ส่งอีเมลแจ้งเตือน (ตอนนี้แจ้งเตือนเฉพาะในเว็บ)
- QR Code เช็กชื่อเข้าร่วมกิจกรรม
- ช่องลายเซ็นหัวหน้าหลักสูตรแบบออนไลน์
- ก่อนใช้งานจริง: เปลี่ยนรหัสผ่านฐานข้อมูลใน `.env`, ตั้ง `APP_DEBUG=false`, ใช้ HTTPS, ลบบัญชีทดลอง

## 7. ข้อควรรู้
- ไฟล์ SQL ใน `database/init/` รัน **ครั้งแรกเท่านั้น** ถ้าแก้ schema ต้อง `docker compose down -v` (ข้อมูลหาย) หรือ ALTER เอง
- รหัสผ่านบัญชีทดลองทั้งหมด: `password123`
- โหมด mock ของ API: รหัส `166404140005`, `166404140006`, `167404150003` มีข้อมูล; ถ้าตั้ง `AUTH_MODE=hybrid` เข้าสู่ระบบด้วยรหัสเหล่านี้ + รหัสผ่าน `rmutsv` ได้
